using System;

namespace _15._2
{
    class Program
    {
        static int Sign(int a)
        {
            int a1;
            //проверка а
            if (a < 0)
                return -1;
            else if (a == 0)
                return  0;
            else if (a > 0)
                return  1;
            return a;

        }
        static void Main(string[] args)
        {
            
            Console.WriteLine("Введите а:");
            int a = int.Parse(Console.ReadLine());
            Console.WriteLine("Sign(a) = " + Sign(a));
            Console.WriteLine("Введите b:");
            int b = int.Parse(Console.ReadLine());
            int c = Sign(a) + Sign(b);
            // int c = Sign(a) + Sign(b);
            
            Console.WriteLine("Sign(a)+Sign(b) = " + c);
            
        }
    }
}
